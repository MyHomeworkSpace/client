import { h, Component } from "preact";


export default class CalendarPane extends Component {
	render(props, state) {
		return <div>
			<p>There are currently no settings for Calendar. If you'd like to add or remove a school account from your Calendar, you can do so from the "Connected school accounts" section of the "Account" pane.</p>
		</div>;
	}
};